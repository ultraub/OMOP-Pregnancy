# SQL Server Environment Configuration Template
# Copy this file to .env and fill in your values

# Java configuration (optional - defaults to system Java)
# Set this if you need a specific Java version
# JAVA_HOME=/path/to/java/home

# Environment selection
OMOP_ENV=SQLSERVER

# SQL Server Connection Settings
SQL_SERVER=ohdsicdmsqlserver.database.windows.net
SQL_DATABASE=tufts
# Authentication method:
# - For Azure SQL Database: MUST use SQL authentication (provide username/password)
# - For on-premise SQL Server: Can use Windows AD (leave blank) or SQL auth
SQL_USER=your_username_here
SQL_PASSWORD=your_password_here
# For Windows AD authentication (on-premise only), leave USER and PASSWORD blank
SQL_PORT=1433

# JDBC Driver Path
SQL_JDBC_PATH=./jdbc_drivers

# Database Schemas
# CDM Schema (where OMOP CDM tables are located)
SQL_CDM_SCHEMA=dbo
SQL_CDM_DATABASE=tufts  # Optional: if CDM is in different database

# Results Schema (where output will be written)
SQL_RESULTS_SCHEMA=dbo
SQL_RESULTS_DATABASE=tufts  # Optional: if results go to different database

# Query Timeout (optional, in seconds)
QUERY_TIMEOUT=3600