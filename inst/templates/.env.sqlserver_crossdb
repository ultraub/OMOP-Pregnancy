# SQL Server with Cross-Database Configuration Template
# For scenarios where CDM and results are in different databases
# Copy this file to .env and update with your settings

# Database type
DB_TYPE=sql server

# Server connection details
# This is your primary connection database (usually where CDM is)
DB_SERVER=your-server.domain.com
DB_DATABASE=cdm_database_name
DB_PORT=1433

# Authentication (choose one method)
# Option 1: Windows AD Authentication
USE_WINDOWS_AUTH=false

# Option 2: Standard SQL Authentication (if USE_WINDOWS_AUTH=false)
DB_USER=your_username
DB_PASSWORD=your_password

# Cross-Database Schema Configuration
# Format: [database_name].[schema_name]

# CDM tables location
CDM_SCHEMA=cdm_database.dbo
VOCABULARY_SCHEMA=cdm_database.dbo

# Results tables location (different database)
RESULTS_SCHEMA=results_database.dbo

# Example configurations:
# 1. Everything in same database, different schemas:
#    CDM_SCHEMA=cdm_schema
#    RESULTS_SCHEMA=results_schema
#
# 2. CDM in one database, results in another:
#    CDM_SCHEMA=OMOP_CDM.dbo
#    RESULTS_SCHEMA=OMOP_Results.dbo
#
# 3. CDM and vocabulary in different databases:
#    CDM_SCHEMA=Clinical.dbo
#    VOCABULARY_SCHEMA=Vocabulary.dbo
#    RESULTS_SCHEMA=Analytics.dbo

# JDBC driver location
JDBC_DRIVER_PATH=jdbc_drivers

# Output configuration (local file output)
OUTPUT_FOLDER=output

# Query timeout (seconds) - increase for large databases
QUERY_TIMEOUT=3600