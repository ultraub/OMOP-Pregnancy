# SQL Server with Cross-Database Configuration Template
# For scenarios where CDM and results are in different databases
# Copy this file to .env and update with your settings

# Environment selection (compatible with previous version)
OMOP_ENV=SQLSERVER

# SQL Server Connection Settings
# This is your primary connection database
SQL_SERVER=your-server.domain.com
SQL_DATABASE=tufts
SQL_PORT=1433

# Authentication (choose one method)
# Option 1: Windows AD Authentication
USE_WINDOWS_AUTH=false

# Option 2: Standard SQL Authentication (if USE_WINDOWS_AUTH=false)
SQL_USER=your_username
SQL_PASSWORD=your_password

# Cross-Database Schema Configuration
# Method 1: Using separate database variables (recommended)
SQL_CDM_DATABASE=cdm_database
SQL_RESULTS_DATABASE=results_database
SQL_CDM_SCHEMA=dbo
SQL_RESULTS_SCHEMA=dbo

# Method 2: Using fully qualified schema names
# SQL_CDM_SCHEMA=cdm_database.dbo
# SQL_RESULTS_SCHEMA=results_database.dbo

# Vocabulary Schema (usually same as CDM)
SQL_VOCABULARY_SCHEMA=dbo

# Example configurations:
# 1. Everything in same database, different schemas:
#    SQL_CDM_SCHEMA=cdm_schema
#    SQL_RESULTS_SCHEMA=results_schema
#
# 2. CDM in one database, results in another:
#    SQL_CDM_DATABASE=OMOP_CDM
#    SQL_RESULTS_DATABASE=OMOP_Results
#    SQL_CDM_SCHEMA=dbo
#    SQL_RESULTS_SCHEMA=dbo
#
# 3. CDM and vocabulary in different databases:
#    SQL_CDM_DATABASE=Clinical
#    SQL_VOCABULARY_DATABASE=Vocabulary
#    SQL_RESULTS_DATABASE=Analytics
#    SQL_CDM_SCHEMA=dbo
#    SQL_VOCABULARY_SCHEMA=dbo
#    SQL_RESULTS_SCHEMA=dbo

# JDBC Driver Path
SQL_JDBC_PATH=./jdbc_drivers

# Output configuration (local file output)
OUTPUT_FOLDER=output

# Query Timeout (seconds) - increase for large databases
QUERY_TIMEOUT=3600