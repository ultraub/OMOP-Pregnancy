# Databricks Environment Configuration Template
# Copy this file to .env and fill in your values

# Environment selection
OMOP_ENV=DATABRICKS

# Databricks Connection Settings
DATABRICKS_SERVER=adb-4068548029743470.10.azuredatabricks.net
DATABRICKS_HTTP_PATH=/sql/1.0/warehouses/ba264cbc36eb86d3
DATABRICKS_TOKEN=your_token_here

# JDBC Driver Path
DATABRICKS_JDBC_PATH=./jdbc_drivers

# Database Schemas
# CDM Schema (catalog.schema format for Databricks)
DATABRICKS_CDM_SCHEMA=omop.data
DATABRICKS_CDM_CATALOG=omop  # Optional: explicitly set catalog if needed

# Results Schema (catalog.schema format for Databricks)
DATABRICKS_RESULTS_SCHEMA=rit_projects.preeclampsia_irb00501176
DATABRICKS_RESULTS_CATALOG=rit_projects  # Optional: explicitly set catalog if needed

# JVM Memory Settings (optional)
JAVA_OPTS=-Xmx8g -XX:MaxDirectMemorySize=4g